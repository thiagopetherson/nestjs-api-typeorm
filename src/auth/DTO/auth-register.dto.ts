import { CreateUserDTO } from "src/user/DTO/create-user.dto";

// Extende o CreateUserDTO (que tem regras que servem para esse DTo aqui também)
export class AuthRegisterDTO extends CreateUserDTO {

  
}